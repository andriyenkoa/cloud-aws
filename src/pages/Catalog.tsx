import ProductCatalog from '../components/Collection/CollectionWrapper';

const Catalog = () => {
	return (
		<main style={{
			padding: '20px'
		}}>
			<ProductCatalog />
		</main>
	);
};

export default Catalog;
